# 2025-06-09 工作日志

## 完成：优惠系统功能升级

### 1. 妹妹管理 - 不参与优惠设置
- **后端**: `girls` 表新增 `exclude_from_discount` 字段
- **前端**: GirlsPage.tsx 添加开关控件，列表显示"无优惠"标签
- **文件**: `db/schema.ts`, `functions/api/girls.ts`, `src/pages/GirlsPage.tsx`

### 2. 会员设置 - 优惠前提价
- **后端**: 新增 `member_configs` 表，包含 `price_markup` 字段
- **前端**: SettingsPage.tsx 会员设置页面添加前提价输入框
- **逻辑**: 计算折扣时先加上前提价金额，再应用折扣
- **文件**: `db/schema.ts`, `functions/api/member-configs.ts`, `src/pages/SettingsPage.tsx`

### 3. 妹妹套餐 - 当日价格
- **后端**: `girl_package_prices` 表新增 `daily_price` 字段
- **前端**: GirlsPage.tsx 价格设置弹窗添加"当日价"输入框（粉色边框区分）
- **逻辑**: 当日价格 > 0 时优先使用，否则使用常规价格
- **文件**: `db/schema.ts`, `functions/api/girl-package-prices.ts`, `src/pages/GirlsPage.tsx`

### 4. 订单价格自动计算
- **后端**: 新增 `calculate-price.ts` API，整合所有价格计算逻辑
- **逻辑**:
  - 优先使用当日价格
  - 不参与优惠的妹妹直接返回原价
  - 其他情况应用会员折扣（含前提价计算）
  - 自动比较并选择最优价格
- **文件**: `functions/api/calculate-price.ts`, `src/pages/OrdersPage.tsx`

### 5. 数据库迁移
- **文件**: `db/migrations/0002_add_discount_features.sql`
- **包含**: 所有新字段和表的创建语句

### 修改文件清单
- db/schema.ts
- db/migrations/0002_add_discount_features.sql
- functions/api/girls.ts
- functions/api/member-configs.ts (新增)
- functions/api/girl-package-prices.ts (新增)
- functions/api/calculate-price.ts (新增)
- src/types/index.ts
- src/hooks/useApi.ts
- src/pages/GirlsPage.tsx
- src/pages/SettingsPage.tsx
- src/pages/OrdersPage.tsx

**提交**: 83b0228

---

## 修复：会员配置API路径

### 问题
会员设置保存失败，报错 `{"success":false,"error":"Not found"}`

### 原因
前端请求路径 `/member-configs` 与后端路由 `/member-config` 不匹配

### 修复
- 修改 `src/hooks/useApi.ts`：
  - GET `/member-configs?storeId=xxx` → `/member-config?storeId=xxx`
  - POST `/member-configs` → `/member-config`

**提交**: 5492c90
