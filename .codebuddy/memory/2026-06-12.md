# 2026-06-12 工作日志

## 订单编辑功能
- 点击订单卡片打开编辑弹窗，可查看和修改订单数据
- 只读信息：订单号、顾客、妹妹、套餐、原价、创建时间、折扣类型
- 可编辑字段：订单状态、预约时间、实付金额、优惠金额、妹妹收入、客服提成、优惠券来源、备注
- 优惠金额编辑时从分转元显示，保存时元转分
- 修改文件: `src/pages/OrdersPage.tsx`
  - `OrderListByDate` 组件增加 `onOrderClick` 回调
  - 订单卡片添加 cursor-pointer 和 hover 效果
  - 新增编辑弹窗状态和 `handleOrderClick`、`handleSaveEdit` 函数

## 完成订单时可选实际服务时长
- 点"完成"弹窗选择：全部完成 / 半小时 / 自定义分钟
- 按比例调整实付金额、妹妹收入、客服提成
- 非全部完成时备注自动追加"实际XX分钟"

## 订单更新余额同步
- 后端 PUT /api/orders 修改：订单更新时自动同步顾客余额变动
- 场景1: finalPrice 变化（如调整时长）→ 按比例退还余额差额，记录 type='refund' 流水
- 场景2: 订单取消 → 退还全部已扣余额，记录退款流水
- 修改文件: `CSOrderSystem-api/src/routes/orders.ts`
- 修复：完成/取消按钮点击事件冒泡导致同时弹出编辑弹窗

## Bug修复
- GirlsPage.tsx: trialPrice 从 string 转 number 提交（commit: 454d415）
- OrdersPage.tsx: 按钮 div 加 stopPropagation 阻止冒泡（commit: 24e1a1c）

## Excel导出数据与实际时长对齐
- 问题：顾客未上满时，导出Excel的"单价"和"课时"仍显示预约值，导致金额对不上
- 修改内容：
  1. orders schema 新增 `actualMinutes` 字段（integer, nullable）
  2. 创建迁移 0010_add_actual_minutes.sql
  3. `handleConfirmComplete` 保存 `actualMinutes` 到数据库
  4. 后端导出 API 返回 `actualMinutes`、`isAdjusted`，并按实际时长折算 `originalPrice`（单价）
  5. 前端导出 Excel：有实际时长时，"课时"显示"XX分钟"，单价按比例折算，新增"妹妹收入"和"客服提成"列
- 修改文件: schema.ts, OrdersPage.tsx, ordersExport.ts, ExportPage.tsx
- 数据库迁移和 API 已部署
- 修复：导出余额查询原来只查 type='consume'，漏掉 refund，导致余额不对。改为查所有流水类型，取订单最后一条流水的 balanceAfter（commit: a5e6f00）

## 上线前安全与逻辑修复（commit: 43d3c40）
1. **orders PUT 白名单**：原来用 `{ ...body }` 直接赋值，存在安全隐患。改为只允许修改指定字段（status, finalPrice, girlIncome, serviceCommission, discountAmount, deductedBalance, remark, appointmentTime, actualMinutes, discountType, discountPercent, storeProfit）
2. **双重复退款修复**：取消订单和价格调整原来用 if+if 可同时触发，改为 if+else if 互斥
3. **deductedBalance 单位**：确认存的是元（非分），与退款逻辑一致，加注释说明
4. **充值金额单位确认**：前端传分，后端直接用，逻辑正确
5. **calculateMemberLevel 改为从DB读取**：recharge.ts 中等级阈值原来硬编码，改为从 member_levels 表读取，管理员修改等级配置后充值计算会同步
6. **前端 Order 类型补全**：新增 actualMinutes, serviceStaffName, dailyPrice discountType, 单位注释

## 登录认证系统（commit: a147eb2）
- 新增 `users` 表（username, PBKDF2 password_hash, salt, storeId, role）
- 迁移文件 0011_add_users_table.sql，数据库已执行
- auth 路由: login/register/init/me，JWT签发7天有效期
- PBKDF2 密码哈希（100000次迭代 + SHA-256）
- JWT HMAC-SHA256 签名验证，JWT_SECRET 在 wrangler.toml vars 中配置
- authMiddleware: 所有 /api/* 路需鉴权，/api/auth/* 跳过
- LoginPage: 登录/初始化模式切换，首次使用时创建管理员账号
- appStore: 新增 token/authUser/isLoggedIn/clearAuth
- useApi + useQueryHooks: 自动带 Authorization header，401时自动清除登录状态
- App.tsx: 未登录显示 LoginPage，已登录正常使用
- SettingsPage: 底部加退出登录按钮
- 部署: API已部署，迁移已执行，代码已推送

## Bug修复：创建账号报错 importKey
- 问题：`hashPassword` 中 `crypto.subtle.importKey` 传了6个参数，多余的 `'PBKDF2'` 字符串导致参数错位，`false` 被当成 keyUsages
- 修复：移除多余的 `'PBKDF2'` 字符串参数，只保留 `{ name: 'PBKDF2' }` 作为 algorithm 参数
- 修改文件: `CSOrderSystem-api/src/routes/auth.ts`

## Bug修复：分析页面和日报无数据
- 问题：AnalysisPage 和 DailyReportPage 直接用原生 fetch() 请求 API，没带 Authorization 头，auth 中间件返回 401
- 修复：
  1. useApi.ts 新增 getCustomerPreferences/getCustomerDetail/getGirlTrends/getDailyReport 方法
  2. AnalysisPage.tsx 改用 useApi hook
  3. DailyReportPage.tsx 改用 useApi hook
- 已构建并部署前端
